const ISSUE_TYPES = {
  COMPLAINT: "Complaint",
  SUPPORT: "Support",
};

const ISSUE_TYPES_LIST = Object.values(ISSUE_TYPES);

export const ISSUE = {
  TYPES: ISSUE_TYPES,
  TYPES_LIST: ISSUE_TYPES_LIST,
};
